var config = {"useorigin":true,"fullAPIurl":"http://domain:port","port":"3002"}
// If useorigin is true, then fullAPIurl  is not required.
// If usorigin is false, then you need to set fullAPIurl like this:
// protocol://hostname:port
// Port variable is always required.
export default {config}
